mixin field(id, colSize, name, text, value, placeholder, autofocus)
  div(class='small-#{colSize} columns')
    if text
      label(for=name) #{text}:
    input(id=id, value=value, type='text', name=name, placeholder=placeholder, autofocus=autofocus)
